# PCS MoveIQ Server Configuration
PORT=4000
DB_PATH=moveiq.db

# Anthropic API key for item identification and pricing
# When missing, the app falls back to mock providers
ANTHROPIC_API_KEY=

# eBay Browse API credentials (optional)
# When missing, comparable search uses mock data only
EBAY_APP_ID=
EBAY_CERT_ID=
